using System;
using System.IO;
using System.Linq;
using System.Reflection;
//using Microsoft.Build.Evaluation;

namespace USBTerminal.Simulator
{
    //class Program
    //{
    //    // https://stackoverflow.com/questions/5498911/run-multiple-instances-with-one-click-in-visual-studio
    //    static void Main(string[] args)
    //    {
    //        // AppDomain.CurrentDomain.GetAssemblies().Where(x => x.FullName.Contains("SolutionName"));
    //        // string directoryName = Path.GetDirectoryName(Assembly.GetExecutingAssembly().Location);
    //        var assemblies = AppDomain.CurrentDomain.GetAssemblies().Select(a => a.FullName).ToList();
    //    }
    //}
}
